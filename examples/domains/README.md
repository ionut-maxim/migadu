# domains example

Shows how to list, get, create, update, and activate domains, and how to fetch DNS records and run diagnostics.

```sh
MIGADU_USER=you@example.com MIGADU_API_KEY=yourkey go run .
```
